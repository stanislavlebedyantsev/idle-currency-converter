const initState = {
  rates: [],
  lastUploadedRates: {}
}

const chartsReducer = (state = initState, action) => {
  let copyState = state
  switch(action.type){
    default:
      return copyState;
  }
}